"""Research Agent — local-first multi-agent CLI for researchers.

The ``__version__`` constant is the runtime source of truth and is
mirrored by ``pyproject.toml [project] version``. ``research
--version`` reads from here so it works in editable installs even
when the distribution metadata isn't built.
"""

from __future__ import annotations

__version__ = "0.5.0"

